language: php

php:
    - 5.3
    - 5.4
    - 5.5

install:
    - composer install
    - vendor/bin/koharness
    - cd /tmp/koharness

script: vendor/bin/phpunit --bootstrap=modules/unittest/bootstrap.php modules/mail/tests
